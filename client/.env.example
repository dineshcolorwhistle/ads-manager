# API URL
VITE_API_URL=http://localhost:5000

# Log out after this many minutes without user input (default in code: 10). Use 120 for 2 hours.
 VITE_IDLE_LOGOUT_MINUTES=120

# Optional: override with exact milliseconds instead of minutes
# VITE_IDLE_LOGOUT_MS=600000
